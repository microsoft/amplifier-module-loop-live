bundle:
  name: live
  version: 0.1.0
  description: Event-driven Amplifier orchestration with a host-owned live runtime
session:
  orchestrator:
    module: loop-live
    source: git+https://github.com/microsoft/amplifier-module-loop-live@main
    config:
      background_delegate: true
